cmake_minimum_required(VERSION 3.16.0)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
#list(APPEND EXTRA_COMPONENT_DIRS managed_components)
# set(PIO_LIB_DIRS ".pio/libdeps/")
# function(add_all_subdirectories_to_include_path ROOT_DIR)
#     file(GLOB ALL_SUBDIRS ${ROOT_DIR}/*)
#     foreach(SUBDIR ${ALL_SUBDIRS})
#         if(IS_DIRECTORY ${SUBDIR})
#             #message(SEND_ERROR "Subdir: ${SUBDIR}")
#             include_directories(${SUBDIR})
#             set(SUBDIR_SRC "${SUBDIR}/src")
#             if(IS_DIRECTORY ${SUBDIR_SRC})
#                 include_directories(${SUBDIR_SRC})
#             endif()
#         endif()
#     endforeach()
# endfunction()
# set(PIO_LIB_DIRS "${PIO_LIB_DIRS}min")
# add_all_subdirectories_to_include_path(${PIO_LIB_DIRS})

project(InkWatchy-personal)
